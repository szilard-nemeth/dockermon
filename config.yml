restart-limit: ${RESTART_LIMIT}
restart-threshold: ${RESTART_THRESHOLD}
restart-reset-period: ${RESTART_RESET_PERIOD}
containers-to-watch: ${CONTAINERS_TO_WATCH}
notification-email-addresses-path: ${NOTIFICATION_EMAIL_RECIPIENT_PATH}
notification-email-server: ${NOTIFICATION_EMAIL_SERVER}
print-all-events: ${PRINT_ALL_EVENTS}